---
interface Props {
  className?: string;
}

const { className = '' } = Astro.props;
---

<div class={`rounded-lg border border-brand-200 bg-brand-50 px-4 py-2 text-sm text-surface-800 ${className}`}>
  Some links on this page are affiliate links - we may earn a commission if you buy, at no extra cost to you.
</div>
