---
interface Props {
  label: string;
}

const { label } = Astro.props;
---

<span class="absolute top-2 left-2 z-10 rounded bg-accent-500 px-2 py-0.5 text-xs font-semibold text-white shadow-sm">
  {label}
</span>
